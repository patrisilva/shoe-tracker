node_modules/
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

.env
.env.local
.env*.local

.DS_Store
*.pem

# Local agent/editor tooling, not part of the app.
# .claude/skills/* are symlinks into .agents/, so they break on any other
# machine; a shared .claude/settings.json can still be committed.
.agents/
skills-lock.json
.claude/settings.local.json
.claude/skills/
npm-debug.log*
yarn-error.log*

# Sign in with Apple private key — never commit this
*.p8
